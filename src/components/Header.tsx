import React from 'react';

const Header: React.FC = () => {
  return (
    <header style={{ backgroundColor: '#3939ac', color: '#fff', padding: '1rem' }}>
      <h1>Tracking Crypto</h1>
    </header>
  );
};

export default Header;
